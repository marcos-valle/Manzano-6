#include <stdio.h>

int main(){
  char nome [20];
  int i;

  for (i = 0; i < 10; i++){
    printf("Digite um nome: ");
    scanf("%s", nome);

    printf("Nome armazenado: %s\n\n", nome);
  }
    return 0;
}
